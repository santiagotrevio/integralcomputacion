# ─────────────────────────────────────────────────────────────
# Copia este archivo como .env y llena los valores reales
# NUNCA subas .env al repositorio
# ─────────────────────────────────────────────────────────────

# Contraseña para el panel de administración
ADMIN_PASSWORD=cambia_esto_por_una_contraseña_segura

# Token secreto para las APIs (genera uno con: openssl rand -hex 32)
API_SECRET_TOKEN=cambia_esto_por_un_token_largo_y_aleatorio

# Puerto del servidor (opcional, default: 3000)
# PORT=3000

# Ruta a la base de datos (opcional, default: inventario.db en raíz)
# DB_PATH=./inventario.db
